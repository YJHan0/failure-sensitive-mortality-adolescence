# Minimal analytical engine (no raw data included in repository)

cat("This repository distributes derived datasets only.\n")
cat("To reproduce calculations, obtain GBD CSV exports following docs/GBD_extraction_guide.md\n")
cat("Then adapt this script to your local data directory.\n")
